import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Items = () => {
  return (
    <View style={styles.container}>
      <Text>Items screen asdasd</Text>
    </View>
  );
};

export default Items;

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
  },
});
